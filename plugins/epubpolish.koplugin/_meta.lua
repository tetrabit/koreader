local _ = require("gettext")

return {
    name = "epubpolish",
    fullname = _("EPUB polish"),
    description = _([[Create a cleaned copy of the currently open EPUB with lightweight, on-device text fixes. Designed for low-power devices.]]),
}
